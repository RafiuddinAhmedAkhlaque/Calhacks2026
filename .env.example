# ScrollStop Server Configuration
# Copy this to server/.env and fill in your values

# Groq API Key for quiz generation (get one free at https://console.groq.com/keys)
GROQ_API_KEY=your_groq_api_key_here

# Server port (default: 3001)
PORT=3001
